# Introducing Multicast

How to send the same data to multiple host?

Unicast: Send many times

Multicast: Send one packet, but the router will copy the packet and send it to the multiple host

为每一个host 维护一个 table
// TODO Does it worth?