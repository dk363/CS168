# Optimizations at the Host

# No offloading

# Stateless Operations

这里指的是需要上下文的操作
(不需要上下文的：检查 checksum)


// TODO RDMA 这里因为没有上 CS61C 所以看的有点云里雾里
// 这里slides 画的图不对 应该是队列而不是 stack
# RDMA(Remote Direct Memory Access)

